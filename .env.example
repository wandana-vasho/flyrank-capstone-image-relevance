# Optional -- omit to run entirely in simulated mode (no cost, no key needed).
# Get a free key at aistudio.google.com (Google account, no card required).
GEMINI_API_KEY=

# Optional -- omit to generate labeled placeholder images locally instead
# of downloading real photos. Get a free key at pexels.com/api.
PEXELS_API_KEY=

DATABASE_URL=sqlite:///./image_relevance.db
